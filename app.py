The app will be available at `http://127.0.0.1:7860`.

## Deploy on Hugging Face Spaces

Just push the repository to a new Space with the `gradio` runtime. The `app.py` file will be executed automatically and the UI will be served publicly.

No API keys are required.